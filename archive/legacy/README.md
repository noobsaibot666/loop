## Legacy Archive

This folder stores legacy static and historical project material that is no longer part of the active React runtime.

Archived here:
- old phase roadmaps
- old phase checklists and readiness reports
- legacy static surfaces that are no longer linked by the active app, including:
  - `admin.html`
  - `membership.html`
  - `how*.html`
  - `privacy*.html`
  - `terms*.html`
  - `coffee.html`

Still active outside this archive:
- `src/`
- `server/`
- `shared/`
- build/config files used by the current React app
- `public/_redirects`
- `public/favicon.png`

Notes:
- footer help/legal/support pages are now React routes:
  - `/how`
  - `/privacy`
  - `/terms`
  - `/coffee`
- Keep this archive for consultation only. Do not restore files into the main runtime path unless a specific regression requires it.
